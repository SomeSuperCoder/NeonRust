use crate::transaction::Transaction;

struct ProgramRuntime {
    process_order: Vec<Vec<Transaction>>
}

impl ProgramRuntime {
    pub fn feed_list(tx_list: Vec<Transaction>) {
        for tx in tx_list {
            
        }
    }
}
